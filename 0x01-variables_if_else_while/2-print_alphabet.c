#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
 * main - Entry will display char in lower case
 * Return: Always 0 (Success)
 */

int main(void)
{
	int n;

	for (n = 'a'; n <= 'z'; n++)
	putchar(n);
	putchar('\n');
	return (0);
}
