#include "main.h"

/**
 * _isupper - to print uppercase
 * @a: char to be checked
 * Return : Always 0 (Success)
 * Return
 */

int _isupper(int a)
{
if (a >= 'A' && a <= 'Z')
return (1);
else
return (0);
}

