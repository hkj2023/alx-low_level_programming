#include "main.h"

/**
 * mul -  to print multiplication of two number
 * @x: first number
 * @y: second number
 * Return: Always 0 (Success).
 */
int mul(int x, int y)
{

	int prod;

	prod = x * y;

	return (prod);
}
