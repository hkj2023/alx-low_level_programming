#include <stdio.h>

/**
 * main - to prints program name
 * @argc: arguments
 * @argv: name of array
 * Return: Always 0 (Success)
 */

int main(int argc __attribute__((unused)), char *argv[])
{
printf("%s\n", argv[0]);
return (0);
}
