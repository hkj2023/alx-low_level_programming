#include "main.h"
#include <stdio.h>

/**
 * main - to prints program name
 * @argc: arguments
 * @argv: name of array
 * Return: Always 0 (Success)
 */

int main(int argc, char *argv[])
{
printf("%s\n", argv[argc * 0]);
return (0);
}
